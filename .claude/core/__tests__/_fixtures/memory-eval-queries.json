[
    {
        "query": "DbContext factory concurrent blazor",
        "expected": ["blazor-dbcontext-factory"],
        "note": "High-importance architectural constraint about Blazor Server + DbContext"
    },
    {
        "query": "integration tests database no mocks",
        "expected": ["integration-test-real-db"],
        "note": "Feedback memory — never mock the database in integration tests"
    },
    {
        "query": "terse responses no summary trailing",
        "expected": ["terse-response-style"],
        "note": "Feedback memory — user wants no trailing summaries"
    },
    {
        "query": "git clean destructive delete untracked",
        "expected": ["destructive-git-guard"],
        "note": "Constraint — always ask before git clean"
    },
    {
        "query": "memory pruning importance supersession retrieval accuracy",
        "expected": ["memory-pruning-improves-retrieval"],
        "note": "Pattern — pruning/supersession improves hit@k"
    },
    {
        "query": "slash command telemetry usage logs",
        "expected": ["command-usage-telemetry"],
        "note": "Pattern — command usage written to telemetry jsonl"
    },
    {
        "query": "worktree isolation feature branch",
        "expected": ["worktree-isolation-pattern"],
        "note": "Workflow — use git worktrees for isolated feature work"
    },
    {
        "query": "secret scanner credential hook write edit",
        "expected": ["secret-scanner-hook"],
        "note": "Constraint — secret scanner runs only as PreToolUse hook on write/edit operations"
    },
    {
        "query": "skill SKILL.md progressive disclosure references",
        "expected": ["skill-progressive-disclosure"],
        "note": "Pattern — heavy skill content moves to references/ subdir"
    },
    {
        "query": "memory decay active work days confidence",
        "expected": ["memory-decay-active-days"],
        "note": "Constraint — decay uses active work days not calendar days"
    },
    {
        "query": "publish public repo manifest allowlist",
        "expected": ["publish-two-repo-workflow"],
        "note": "Workflow — publish:public uses manifest allowlist"
    },
    {
        "query": "sqlite FTS5 fulltext search fallback json scan",
        "expected": ["sqlite-fts5-backend"],
        "note": "Constraint — FTS5 only works with better-sqlite3; falls back to JSON scan"
    },
    {
        "query": "importance floor retention low importance memory",
        "expected": ["importance-retention-floor"],
        "note": "Pattern — importance 1-5 affects decay floor"
    },
    {
        "query": "hard gate prerequisite command sequence",
        "expected": ["hard-gate-prereq"],
        "note": "Workflow — create commands enforce prerequisite doc chain"
    },
    {
        "query": "agent memory inbox draft review promotion",
        "expected": ["inbox-staging-protocol"],
        "note": "Workflow — sub-agents drop drafts to inbox, main agent promotes"
    }
]
